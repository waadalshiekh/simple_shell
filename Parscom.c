#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int parscom(int input, char *comm[])
    for (int i = 0; comm[i] != NULL; i++) //input here from user
        //here we should put the result
    return = result;
}
